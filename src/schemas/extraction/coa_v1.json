{
  "name": "coa_v1",
  "version": "1.0.0",
  "required_fields": [
    "product_type",
    "supplier_name",
    "lot_number",
    "coa_date",
    "lead",
    "cadmium",
    "moisture_percentage"
  ],
  "fields": {
    "product_type": {
      "type": "array",
      "description": "Product type from COA",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string"},
          "confidence": {"type": "number"}
        }
      }
    },
    "supplier_name": {
      "type": "array",
      "description": "Supplier legal name",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string"},
          "confidence": {"type": "number"}
        }
      }
    },
    "lot_number": {
      "type": "array",
      "description": "Supplier lot number",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string"},
          "confidence": {"type": "number"}
        }
      }
    },
    "coa_date": {
      "type": "array",
      "description": "COA issue date",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string", "format": "date"},
          "confidence": {"type": "number"}
        }
      }
    },
    "lead": {
      "type": "array",
      "description": "Lead value and unit",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string"},
          "confidence": {"type": "number"}
        }
      }
    },
    "cadmium": {
      "type": "array",
      "description": "Cadmium value and unit",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "string"},
          "confidence": {"type": "number"}
        }
      }
    },
    "moisture_percentage": {
      "type": "array",
      "description": "Moisture percentage value",
      "items": {
        "type": "object",
        "required": ["value", "confidence"],
        "properties": {
          "value": {"type": "number"},
          "confidence": {"type": "number"}
        }
      }
    }
  },
  "output_contract": {
    "field_shape": {
      "type": "array",
      "item_shape": {
        "value": "any",
        "confidence": "float"
      }
    }
  }
}
